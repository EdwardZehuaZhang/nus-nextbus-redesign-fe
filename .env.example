# Backend Gateway API URL - handles ALL external API calls securely:
# - NUS NextBus API (bus stops, arrivals, routes, locations, etc.)
# - Google Maps API (routes, places, directions)
# - LTA DataMall API (public bus data)
# Local development: http://localhost:3000
# Production: https://your-backend-gateway.com
EXPO_PUBLIC_BACKEND_API_URL=http://localhost:3000

# Build-time secret (MUST be set via CI/CD secrets, not in .env files)
# This is just a placeholder - use environment variables or EAS secrets
SECRET_KEY=

# Example typed vars
VAR_NUMBER=10
VAR_BOOL=true

# Public Builder key (public, safe to commit if needed)
PUBLIC_BUILDER_KEY=

# Google Maps API Key - ONLY needed for map display components
# The backend handles all Google Maps API calls (routes, places, directions)
# ⚠️ SECURITY: Restrict this key in Google Cloud Console:
#   - Application restrictions: Add iOS bundle ID and Android package name
#   - API restrictions: Enable only Maps SDK (iOS, Android, JavaScript)
# ⚠️ DO NOT commit real values - use .env.local or CI/CD secrets
EXPO_PUBLIC_GOOGLE_MAPS_API_KEY=

# IMPORTANT SECURITY NOTES:
# 1. NEVER commit .env.development, .env.production, or .env.staging to git
# 2. Only .env.example should be in version control
# 3. Real API keys must be:
#    - Stored in .env.local (git-ignored) for local dev
#    - Set via EAS secrets for builds: eas secret:create --scope project --name KEY_NAME
#    - Set via CI/CD environment variables for deployments
# 4. Google Maps key should be restricted by platform (iOS/Android/Web)
# 5. Backend API keys (NUS, LTA) should NEVER appear in frontend code
